using UnityEngine;

public class TutorialManager : MonoBehaviour
{

    // State
    bool firstSpawnerAggroed;
    bool firstWeaponCollected;
    bool firstCollectibleCollected;
    bool firstSurvivorCocoonImpact;

    // Update is called once per frame
    void Update()
    {
        
    }
}
